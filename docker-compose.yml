services:
  youtube-summarizer:
    build: .
    container_name: youtube-summarizer
    ports:
      - "8501:8501"
    volumes:
      - ./config.py:/app/config.py
    env_file:
      - .env
    restart: unless-stopped
